# 大研訂購系統 環境變數設定
# 複製此檔為 .env.local 並填入實際值
# cp .env.example .env.local

# Google Apps Script Web App URL（部署後取得）
REACT_APP_GAS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# 寫入驗證 Token（在 Apps Script 中設定，用於防止未授權寫入）
REACT_APP_WRITE_TOKEN=your_write_token_here
